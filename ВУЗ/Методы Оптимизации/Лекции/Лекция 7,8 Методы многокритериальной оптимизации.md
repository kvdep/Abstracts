## 1. Постановка задачи
Пусть дана вектор-функция критериев:
$$ f(x) = (f_1(x), f_2(x), ..., f_m(x)), \quad x \in X $$
Где $X$ — допустимое множество решений. Задача заключается в одновременной максимизации (или минимизации) всех критериев.

### Определение оптимальности по Парето
Точка $x^*$ называется **эффективной** или **оптимальной по Парето**, если не существует такой точки $x \in X$, для которой:
$$ \forall i: f_i(x) \geq f_i(x^*) \quad \text{и} \quad \exists i_0: f_{i_0}(x) > f_{i_0}(x^*) $$
*(Интуитивно: нельзя улучшить один критерий, не ухудшив при этом хотя бы один другой).*

---

## 2. Методы решения (Методы свертки)

Проблема многокритериальности обычно решается сведением задачи к однокритериальной путем введения функции свертки $f_0 = F(f_1, ..., f_m)$.

### I. Метод линейной свертки
Критерии суммируются с весовыми коэффициентами:
$$ f_0(x) = \sum_{i=1}^{m} \alpha_i f_i(x) \to \max_{x \in X} $$
Где $\alpha_i \geq 0$ и обычно $\sum \alpha_i = 1$.

> **💡 Комментарий эксперта:**
> **Теорема Карлина:** Если множество допустимых оценок выпукло, то для любой оптимальной по Парето точки $x^*$ существует такой вектор весов $\alpha \geq 0$, что $x^*$ является решением задачи максимизации линейной свертки.
>
> *Пример из финансов (упомянут в записях):* Оптимизация портфеля (задача Марковица).
> Мы хотим максимизировать доходность $\mu_p(x)$ и минимизировать риск (дисперсию) $\sigma_p^2(x)$.
> Свертка: $\mu_p(x) - \alpha \sigma_p(x) \to \max$.

### II. Оценка по наихудшему критерию (Метод Гермейера)
Максимизируем минимальный взвешенный критерий (принцип гарантированного результата):
$$ f_0(x) = \min_{i=1...m} (\alpha_i f_i(x)) \to \max_{x \in X} $$
В экономике это может интерпретироваться как попытка "подтянуть" самый отстающий показатель. Например, если $n_k$ — количество деталей $k$-го типа, то количество собранных комплектов равно $\min (n_k / \text{норму})$.

---

## 3. Практический пример (Сквозная задача)

Рассмотрим задачу с двумя критериями:
1. $f_1(x) = 4x_1 + x_2 \to \max$
2. $f_2(x) = x_1 + 2x_2 \to \max$

**Ограничения ($X$):**
$$
\begin{cases}
x_1 + x_2 \leq 7 \\
x_1 \leq 5 \\
x_2 \leq 4 \\
x_1, x_2 \geq 0
\end{cases}
$$

### Шаг 1: Графический анализ и множество Парето
Построим многоугольник допустимых решений $X$ в координатах $(x_1, x_2)$. Вершины многоугольника:
- $O(0,0)$
- $A(0,4)$
- $B(3,4)$ (пересечение $x_2=4$ и $x_1+x_2=7$)
- $C(5,2)$ (пересечение $x_1=5$ и $x_1+x_2=7$)
- $D(5,0)$

Теперь отобразим множество $X$ в пространство критериев $Y$ (отображение $f: X \to Y$).
Матрица преобразования: $\begin{pmatrix} 4 & 1 \\ 1 & 2 \end{pmatrix}$.

**Координаты вершин в пространстве критериев $(f_1, f_2)$:**
- $A(0,4) \to A': (4\cdot0+4, 0+2\cdot4) = (4, 8)$
- $B(3,4) \to B': (12+4, 3+8) = (16, 11)$
- $C(5,2) \to C': (20+2, 5+4) = (22, 9)$
- $D(5,0) \to D': (20+0, 5+0) = (20, 5)$
- $O(0,0) \to O': (0, 0)$

**Множество Парето (эффективный фронт):**
На графике в пространстве критериев (рис. `3.jpg`, `4.jpg`) видно, что "верхняя правая" граница — это отрезок $B'C'$.
- $f_1$ максимально в точке $C'$ (значение 22).
- $f_2$ максимально в точке $B'$ (значение 11).
Любая точка на отрезке $BC$ в пространстве $X$ является оптимальной по Парето.

### Шаг 2: Решение методом линейной свертки
Пусть веса $\alpha_1 = 1, \alpha_2 = 0.5$.
Целевая функция:
$$ f_0 = 1 \cdot (4x_1 + x_2) + 0.5 \cdot (x_1 + 2x_2) = 4.5x_1 + 2x_2 \to \max $$
Градиент этой функции направлен круче к оси $x_1$. Максимум достигается в вершине $C(5,2)$.

---

## 4. Метод последовательных уступок
Алгоритм:
1. Ранжируем критерии по важности (например, $f_2$ важнее $f_1$).
2. Ищем максимум самого важного критерия: $f_2 \to \max$. Получаем $f_2^*$.
3. Задаем допустимую **уступку** (например, 5% или $n_1\%$).
4. Ищем максимум следующего критерия $f_1$ при условии, что значение первого не хуже, чем $f_2^* (1 - \frac{n_1}{100})$.

**Пример:**
1. $f_2(x) \to \max$. Оптимум в точке $B(3,4)$, $f_2^{max} = 11$.
2. Уступка 5%. Новое ограничение: $f_2(x) \geq 11 \cdot (1 - 0.05) = 10.45$.
3. Задача: $f_1(x) = 4x_1 + x_2 \to \max$ при условии $x \in X$ И $x_1 + 2x_2 \geq 10.45$.

Решение находится на пересечении границы уступки $x_1 + 2x_2 = 10.45$ и границы множества $x_1 + x_2 = 7$.
$$
\begin{cases}
x_1 + 2x_2 = 10.45 \\
x_1 + x_2 = 7
\end{cases}
\Rightarrow x_2 = 3.45, \quad x_1 = 3.55
$$
Решение: $x^* = (3.55, 3.45)$.

---

## 5. Метод ограничений
Выбирается один главный критерий, остальные переводятся в разряд ограничений (задается нижний порог $l_i$).

**Пример:**
Главный критерий $f_2 \to \max$. Ограничение на первый: $f_1(x) \geq 19$.
Система:
$$
\begin{cases}
4x_1 + x_2 = 19 \quad (\text{граница } f_1) \\
x_1 + x_2 = 7 \quad (\text{граница } X)
\end{cases}
$$
Вычитаем: $3x_1 = 12 \Rightarrow x_1 = 4$. Тогда $x_2 = 3$.
Решение: $x^* = (4,3)$.

---

## 6. Метод приоритетов
Критерии жестко упорядочиваются: $f_1 \succ f_2 \succ ... \succ f_m$.
1. Ищем множество $X_1$ оптимальных решений для $f_1$.
2. Если в $X_1$ одна точка — это ответ.
3. Если точек много, ищем среди них ту, которая максимизирует $f_2$, сужая множество до $X_2$, и так далее.

---

## 7. Метод идеальной точки
Строится "идеальная точка" $f^0$ в пространстве критериев, координаты которой — это безусловные максимумы каждого критерия по отдельности:
$$ f^0 = (\max f_1, \max f_2) = (22, 11) $$
*(22 взято из точки $C'$, 11 взято из точки $B'$)*.

Задача: найти точку $f(x)$ из множества Парето, ближайшую к $f^0$ в смысле выбранной метрики $\rho$.

### Вариант А: Евклидова метрика ($\rho_2$)
Минимизируем квадрат расстояния:
$$ (f_1 - 22)^2 + (f_2 - 11)^2 \to \min_{f \in B'C'} $$

Уравнение прямой $B'C'$ (проходит через $(16,11)$ и $(22,9)$):
$$ \frac{f_1 - 16}{22 - 16} = \frac{f_2 - 11}{9 - 11} \Rightarrow \frac{f_1-16}{6} = \frac{f_2-11}{-2} \Rightarrow f_1 + 3f_2 = 49 $$

Геометрически кратчайшее расстояние — перпендикуляр из $f^0$ к прямой $B'C'$.
Вектор нормали прямой: $(1, 3)$. Уравнение перпендикуляра из $(22,11)$:
$$ \frac{f_1 - 22}{1} = \frac{f_2 - 11}{3} \Rightarrow 3f_1 - f_2 = 3\cdot22 - 11 = 55 $$

Решаем систему:
$$
\begin{cases}
f_1 + 3f_2 = 49 \\
3f_1 - f_2 = 55
\end{cases}
$$
Умножим второе на 3: $9f_1 - 3f_2 = 165$. Сложим с первым:
$10f_1 = 214 \Rightarrow f_1^* = 21.4$.
$f_2^* = 3(21.4) - 55 = 64.2 - 55 = 9.2$.
Оптимальная точка в критериях: **(21.4, 9.2)**.
Восстанавливаем $x$ (решая систему $4x_1+x_2=21.4$ и $x_1+2x_2=9.2$):
**$x^* = (4.8, 2.2)$**.

### Вариант Б: Метрика Манхэттена ($\rho_1$)
$$ |f_1 - 22| + |f_2 - 11| \to \min $$
Решение ищется касанием линии уровня $|x|+|y|=C$ (ромб) с множеством Парето.

### Вариант В: Метрика Чебышева ($\rho_\infty$)
$$ \max \{ |f_1 - 22|, |f_2 - 11| \} \to \min $$
Линии уровня — квадраты. Решение находится на пересечении биссектрисы угла из идеальной точки и фронта Парето:
Условие на биссектрисе (для нашего квадранта): $-(f_1 - 22) = -(f_2 - 11) \Rightarrow f_1 - f_2 = 11$.
Система с прямой $B'C'$:
$$
\begin{cases}
f_1 - f_2 = 11 \\
f_1 + 3f_2 = 49
\end{cases}
$$
Вычитаем: $4f_2 = 38 \Rightarrow f_2 = 9.5$.
$f_1 = 20.5$.
Находим $x$: **$x^* = (4.5, 2.5)$**.

---

## 8. Метод анализа иерархий (МАИ / AHP)
*(Основано на изображении `1.jpg`)*

Используется для вычисления весовых коэффициентов критериев на основе экспертных оценок.
Пусть $C_1, ..., C_n$ — критерии.
Эксперт заполняет **матрицу парных сравнений** $A$:
$$ A = \begin{pmatrix} 1 & a_{12} & \dots & a_{1n} \\ a_{21} & 1 & \dots & a_{2n} \\ \vdots & \vdots & \ddots & \vdots \\ a_{n1} & a_{n2} & \dots & 1 \end{pmatrix} $$
Где $a_{ij}$ — оценка того, во сколько раз критерий $i$ важнее критерия $j$.
Свойства: $a_{ji} = 1/a_{ij}, a_{ii} = 1$.

**Идея:**
Если матрица идеально согласована (эксперт не противоречит себе: $a_{ik} = a_{ij} \cdot a_{jk}$), то веса критериев $w = (w_1, ..., w_n)^T$ находятся как собственный вектор, соответствующий максимальному собственному числу:
$$ Aw = n \cdot w $$
В реальности матрица редко бывает идеально согласованной, и точное решение задачи на собственные числа ($Aw = \lambda_{max}w$) вручную найти сложно.

На практике используются 4 метода приближенного вычисления весов $w_i$ (нормализованных так, чтобы $\sum w_i = 1$):

#### 1. Метод нормировки строчных сумм
Суммируем элементы каждой строки и делим на общую сумму всех элементов матрицы. Самый грубый метод.
$$ w_i = \frac{\sum_{j=1}^{n} a_{ij}}{\sum_{i=1}^{n} \sum_{j=1}^{n} a_{ij}} $$

#### 2. Метод суммы обратных величин (по столбцам)
Веса пропорциональны обратным величинам сумм элементов столбцов (или строк, в зависимости от ориентации матрицы).
$$ w_i = \frac{1 / \sum_{k=1}^{n} a_{ki}}{\sum_{j=1}^{n} \left( 1 / \sum_{k=1}^{n} a_{kj} \right)} $$
*(Примечание: в рукописи индексы могут выглядеть иначе, но смысл — нормировка обратных сумм).*

#### 3. Метод среднего арифметического нормированных столбцов (Самый популярный)
Сначала каждый элемент делится на сумму своего столбца (нормировка столбцов), затем ищется среднее арифметическое по строке.
$$ w_i = \frac{1}{n} \sum_{j=1}^{n} \frac{a_{ij}}{\sum_{k=1}^{n} a_{kj}} $$

#### 4. Метод среднего геометрического (Метод наименьших квадратов)
Считается наиболее точным после метода собственных векторов. Находим корень $n$-й степени из произведения элементов строки и нормируем полученные числа.
$$ w_i = \frac{\sqrt[n]{\prod_{j=1}^{n} a_{ij}}}{\sum_{k=1}^{n} \sqrt[n]{\prod_{j=1}^{n} a_{kj}}} $$

> **💡 Комментарий эксперта:**
> Для программной реализации (Production) лучше всего использовать **Метод 4 (Геометрическое среднее)** или честный поиск собственного вектора (`numpy.linalg.eig`), так как они дают наиболее согласованные результаты при наличии ошибок в суждениях эксперта.


---

## 9. Целевое программирование (Goal Programming)
Упомянуто в конце. Суть метода: задаются целевые значения для каждого критерия, и минимизируется сумма отклонений (дефицит $d_i^-$ и избыток $d_i^+$) от этих целей.
$$ \sum (d_i^+ + d_i^-) \to \min $$






